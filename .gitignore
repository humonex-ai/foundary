# Secrets
.env

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
build/
dist/

# uv
.uv/

# Legacy local state + its migration backup (record.json is the tracked record).
.foundry-state.json*
projects/*/.foundry-state.json*

# Generated artifacts (per-project output; D-008). The dogfood reference
# projects are committed; other generated runs are not.
projects/*
!projects/foundry/
!projects/ai-investment-firm/
!projects/simple-todo-app/

# skill web-upload bundle (regenerate with: cd skills && zip -r foundry-spec.zip foundry-spec)
skills/*.zip
